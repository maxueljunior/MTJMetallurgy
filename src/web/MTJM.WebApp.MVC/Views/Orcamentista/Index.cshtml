@{
    Layout = "_Layout";
}

@if (TempData["SuccessMessage"] is not null)
{
    <input id="successMessage" value="@TempData["SuccessMessage"]" hidden />
}

@if (TempData["ErrorMessage"] is not null)
{
    <input id="errorMessage" value="@TempData["ErrorMessage"]" hidden />
}

<div class="card my-2 shadow-lg bg-body-tertiary rounded">
    <div class="card-header">
        <div class="row">
            <div class="col-6 col-md-6 align-items-center">
                <h1 class="fs-2 fw-normal">Orçamentista</h1>
            </div>

            <div class="col-6 col-md-6 d-flex align-items-center justify-content-end">
                @if (_claimsHelper.UserHaveAccess(PermissionsType.Orcamentista, PermissionValue.Create))
                {
                    <a class="btn btn-outline-primary" asp-action="Create" asp-controller="Orcamentista">Create Orçamentista</a>
                }
            </div>
        </div>
    </div>
    <div class="card-body">


        <div class="row">
            <div class="col-12">
                <partial name="./Partial/_PartialTableOrcamentista" />
            </div>
        </div>

    </div>
</div>

@section Scripts {
    <script src="~/Views/Orcamentista/Index.cshtml.js"></script>
}